import React from 'react';
import { View, Text } from 'react-native';
import { createStackNavigator } from '@react-navigation/stack';

const referStack = () => {
    return (
        <View>
            <Text>referStack</Text>
        </View>
    );
}

export default referStack;